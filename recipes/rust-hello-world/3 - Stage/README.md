# Rust Hello World - Stage
This is a staging environment for [Rust Hello World (info + deploy)](https://app.zerops.io/recipes/rust-hello-world?environment=stage) recipe on [Zerops](https://zerops.io).

<!-- #ZEROPS_EXTRACT_START:intro# -->
Pre-production staging environment running the same `prod` build pipeline as production. Single container with minimal resources for functional and integration testing before going live. Mirrors the production configuration without horizontal scaling costs.
<!-- #ZEROPS_EXTRACT_END:intro# -->
